# Capstone Project 

descripcion....

instalar git-lfs para subir los modelos o archivos grandes
`git lfs install`

Trackear archivos grandes
`git lfs track "*.pkl"`
`git lfs track "*.joblib"`
`git lfs track "ml/data/**"`